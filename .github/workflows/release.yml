name: Release

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build:
    name: Build distributions
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-wasip1
      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
      - name: Cache CPython WASI assets
        uses: actions/cache@v4
        with:
          path: assets/cpython-wasi/downloads
          key: cpython-wasi-${{ runner.os }}-${{ hashFiles('assets/cpython-wasi/manifest.json') }}
      - name: Prepare CPython WASI assets
        run: python3 scripts/prepare_cpython_wasi_assets.py
      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install maturin
      - name: Build wheel and sdist
        run: python scripts/build_pypi_artifacts.py --out-dir dist
      - name: Upload distributions
        uses: actions/upload-artifact@v4
        with:
          name: pypi-dist
          path: dist/*
          if-no-files-found: error

  publish:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
    environment:
      name: pypi
      url: https://pypi.org/p/agentbox
    steps:
      - name: Download distributions
        uses: actions/download-artifact@v4
        with:
          name: pypi-dist
          path: dist
      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
